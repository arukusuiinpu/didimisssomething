architectury {
    common(rootProject.enabled_platforms.split(","))
}

tasks.withType(Jar).configureEach {
    mustRunAfter(':autoupdater:copyJarToResources')
}

tasks.named('processResources') {
    dependsOn(':autoupdater:copyJarToResources')
}